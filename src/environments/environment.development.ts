//ng serve --configuration=development
export const environment = {
    production: false,
    apiUrl: 'http://127.0.0.1:8000/api/v1',
  };

    /*muchas librerías y lógicas de código revisan la variable production 
  para optimizar o cambiar comportamientos (por ejemplo, mostrar mensajes 
  de depuración solo en desarrollo).*/
